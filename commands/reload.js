const Discord = require('discord.js');

exports.run = (bot, message, args, text) => {
    if (message.author.id != "448560475987509268") return;
    message.channel.send('```* Le fait que vous ayez tapé cette commande, vous remplit avec de la DÉTERMINATION.\n* The action of typing this command, fills you with DETERMINATION.\n```')
    function function2() {
        delete require.cache[require.resolve(`C:/Users/guill/OneDrive/Documents/Bureau/Bots/#GuiguiBot/index.js`)];
    }
    setTimeout(function2, 2000);
}